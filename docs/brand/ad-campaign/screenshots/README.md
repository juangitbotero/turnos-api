# Drop app screenshots here

Name them by screen so the generator can address them:

    01-map.png          Shifts — map view with pins and the price callout
    02-list.png         Shifts — list view with the €/hr pills
    03-profile.png      My Profile — rating, badges, availability toggle
    04-my-shifts.png    My Shifts — pending / completed, "received" confirmation

**PNG, portrait, at device resolution** (whatever the phone exports — bigger is
better, the generator downsamples). Do not crop or annotate them; the callouts
are cut from the full screen at build time.

⚠️ Screens showing rows prefixed `Demo ·` come from the video seeder and should
not appear in an ad — see `docs/go-live-cleanup.md`.
