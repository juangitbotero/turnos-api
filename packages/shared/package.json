{
  "name": "@turnos/shared",
  "version": "0.0.1",
  "private": true,
  "description": "Shared types, constants and utilities across all Turnos apps",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc --project tsconfig.build.json",
    "lint": "eslint src --ext .ts",
    "check-types": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "5.9.2"
  }
}
